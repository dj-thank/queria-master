# Build status

- Source layout: complete MVP scaffold
- DuckDB native path: Queria CLI/Python subprocess removed; embedded DuckDB direct DuckLake ATTACH implemented
- Frontend static type check in this container: dependencies are not installed, so imports cannot resolve here
- Rust compile: not run in this Linux container because Rust/Cargo and Windows MSVC toolchain are unavailable
- Windows installers: generated by `scripts/build-windows.ps1` or `.github/workflows/windows-build.yml`
- JSIC normalizer: syntax checked and tested with a small four-level sample
- JSON configuration: validated

The repository intentionally does not contain `codex.exe`; the Windows build fetches pinned Codex 0.146.0 and bundles it as a Tauri resource.
