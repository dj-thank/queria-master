# data ディレクトリ

`01_初回セットアップ.bat` または `bootstrap.sh` の実行後、ここに `queria_master.duckdb` が生成されます。

公開法人データ本体は ZIP に固定同梱していません。Queria 側の更新を初回実行時に取得し、情報通信業だけを抽出してローカル DuckDB に保存するためです。これにより、古いスナップショットを配布し続ける状態を避けます。
