# このファイルは自動読込されません。必要な値をOSの環境変数として設定する例です。
# 通常は `queria login` を使うため、QUERIA_TOKEN をここへ保存しないでください。
QUERIA_NO_TELEMETRY=1

# パッケージとしてインストールした場合のDB保存先を明示するときだけ使用します。
# QUERIA_MASTER_HOME=C:\path\to\queria-master
